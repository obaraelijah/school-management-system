import { NavLink } from 'react-router-dom';
const Logo = () => {
  return (
    <NavLink className={'text-lg md:text-2xl'}>
      smart<span>Ed</span>Connect
    </NavLink>
  );
};

export default Logo;
